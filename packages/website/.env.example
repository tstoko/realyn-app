# URL of the dashboard app (for login redirect)
# Production builds use https://dashboard.realyn.app by default.
# For local dev, copy to .env.development and set:
#   VITE_DASHBOARD_URL=http://localhost:3001

# Firebase auth domain (set to your custom domain for cross-subdomain auth)
# VITE_FIREBASE_AUTH_DOMAIN=realyn.app
