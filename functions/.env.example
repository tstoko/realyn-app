# Copy to .env for local Functions / emulator (never commit real secrets).
# Gen2 deployed functions: set secrets with `firebase functions:secrets:set RESEND_API_KEY`
# and set DASHBOARD_URL on each Cloud Run service (or use GCP console → Cloud Functions → edit → Variables).

RESEND_API_KEY=
DASHBOARD_URL=https://dashboard.realyn.app
