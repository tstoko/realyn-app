/**
 * Policy version written onto seeded demo `users` docs so login skips the consent modal.
 * Keep in sync with `CURRENT_POLICY_VERSION` in packages/shared/src/config/legal.ts.
 */
export const DEMO_SEEDED_POLICY_VERSION = "2026-02-23";
